# Required
DATABASE_URL=postgres://pas:pas_dev_password@localhost:5432/pas

# Optional
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
SEARCH_INDEX_PATH=./search_index
RUST_LOG=info
# OTLP_ENDPOINT=http://localhost:4318
# API_TOKEN=replace-with-a-strong-token-to-enable-bearer-auth
# CORS_ORIGINS=https://app.example.com,https://admin.example.com
# HL7V2_MLLP_BIND=0.0.0.0:2575   # enables the MLLP TCP listener for HL7 v2 ADT
# HL7V2_OUTBOUND_PEER=emr.example.com:2575   # downstream MLLP receiver for outbound ADT^A01/A02/A03
